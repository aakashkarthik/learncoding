#include <stdio.h>
#include <limits.h>

int main(void)
{
	printf("max char: %d\n", CHAR_MAX);
	printf("max short: %d\n", SHRT_MAX);
	printf("max int: %d\n", INT_MAX);
	printf("max long: %ld\n", LONG_MAX);
	//Therefore (a)char, (b)short, (c)short, (d)int; in the problem.
}
